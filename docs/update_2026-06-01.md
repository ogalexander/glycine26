# USER_MANUAL update notes (2026-06-01)

Scope checked:
- `USER_MANUAL.txt`
- `analysis/scripts/*.py`
- `analysis/configs/*.py`
- `analysis/notebooks/*.ipynb`

## Summary
The manual is largely consistent with the current codebase. I found a small set of updates worth making so it matches the repository state more closely.

## Recommended updates

1. Update remote-path status in Section 2
- Current manual text (`USER_MANUAL.txt`, lines 32-33) says remote paths are placeholders.
- Current code (`analysis/scripts/config.py`) already defines concrete remote paths under `/asap3/flash/gpfs/fl24/2026/data/11022188/...`.
- Suggested wording change:
  - Replace "Remote paths are still placeholders; fill them into config.py once the production location is confirmed."
  - With "Remote paths are preconfigured in config.py; verify they match the active FLASH online-cluster location before production runs."

2. Expand notebook inventory in Section 7
- Manual currently lists only:
  - `diagnostics.ipynb`
  - `tof_inspect.ipynb`
  - `vls_inspect.ipynb`
- Repository also contains:
  - `check_train_alignment.ipynb`
  - `demo.ipynb`
- Suggested update: add these notebooks with one-line descriptions so users know they exist.

3. Clarify data-root expectation in Section 3
- Manual directory layout lists `11022188/` as if always present.
- In this workspace snapshot, that folder is not committed in the repo tree (expected for large data).
- Suggested wording tweak:
  - Indicate `11022188/` is the expected runtime data root (local/cluster), not necessarily version-controlled in git.

4. Make Step 1 wording less absolute
- Section 4 says combined H5 build is "already done for the test data".
- For fresh environments this may not be true.
- Suggested wording change:
  - "If combined test files are missing, run: write_h5_test_config1.py and write_h5_test_config2.py."

## What did not need changes
- Script API names and signatures in `analysis/scripts/` are consistent with the manual.
- Aggregate modes/config examples in Section 8 align with `analysis/configs/` and `compute_aggregates.py`.
- Covariance/ADMM references in Section 9 match existing notebooks and modules.
